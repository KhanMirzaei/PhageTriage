#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
export PATH="${ROOT}/envs/viralcomplete/bin:${PATH}"
exec "${ROOT}/envs/viralcomplete/bin/python" "${ROOT}/src/viralComplete/bin/viralcomplete" "$@"

