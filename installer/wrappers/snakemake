#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
export PATH="${ROOT}/envs/phagetriage/bin:${PATH}"
exec "${ROOT}/envs/phagetriage/bin/snakemake" "$@"
